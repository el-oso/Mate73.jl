import{_ as s,o as i,c as a,ao as t}from"./chunks/framework.BdOmiPte.js";const k=JSON.parse('{"title":"Small compiled programs","description":"","frontmatter":{},"headers":[],"relativePath":"trim.md","filePath":"trim.md","lastUpdated":null}'),n={name:"trim.md"};function o(l,e,h,r,p,d){return i(),a("div",null,[...e[0]||(e[0]=[t(`<h1 id="Small-compiled-programs" tabindex="-1">Small compiled programs <a class="header-anchor" href="#Small-compiled-programs" aria-label="Permalink to &quot;Small compiled programs {#Small-compiled-programs}&quot;">​</a></h1><p><strong>A tool named <code>juliac</code> turns Julia code into a small program. This package is written to work inside one.</strong></p><p>Think of packing for a trip with one small bag. You take only the clothes you will wear. You leave the rest at home.</p><p><code>juliac</code> does the same with code. The option <code>--trim=safe</code> keeps only the code the program can reach. It then refuses to build if it cannot work out which function a call will use. A file reader normally decides that while it runs. So the design of this package had to change.</p><h2 id="How-to-use-it" tabindex="-1">How to use it <a class="header-anchor" href="#How-to-use-it" aria-label="Permalink to &quot;How to use it {#How-to-use-it}&quot;">​</a></h2><p>The program needs a <code>@main</code> function at the top level of the file. <code>juliac</code> copies the file into a fresh place, so a <code>module</code> around it does not work.</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> MATTE73</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@main</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)(args</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Vector{String}</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Cint</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    out </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Core</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">stdout</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                     # not Base.stdout</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    a </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> matread</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">matopen</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(args[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]), args[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], Matrix{Float64})</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    println</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(out, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;n=&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">length</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(a), </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot; sum=&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">sum</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(a))</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Cint</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span></code></pre></div><p>Use <code>Core.stdout</code>. <code>Base.stdout</code> can hold any kind of output, so the build cannot work it out.</p><p>Build it:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>julia --project=. \\</span></span>
<span class="line"><span>  $(julia -e &#39;print(joinpath(Sys.BINDIR, &quot;..&quot;, &quot;share&quot;, &quot;julia&quot;, &quot;juliac&quot;, &quot;juliac.jl&quot;))&#39;) \\</span></span>
<span class="line"><span>  --output-exe matsum --experimental --trim=safe entry.jl &lt; /dev/null</span></span></code></pre></div><p><strong>The <code>&lt; /dev/null</code> part is needed.</strong> Without it the Julia launcher can fail while it reads its input, after the build has already worked. That looks like a broken build. It is not.</p><p>The file <code>juliac/build.jl</code> in this repository does all of the above. It then runs the program and compares the answer against normal Julia code.</p><h2 id="What-this-costs-the-interface" tabindex="-1">What this costs the interface <a class="header-anchor" href="#What-this-costs-the-interface" aria-label="Permalink to &quot;What this costs the interface {#What-this-costs-the-interface}&quot;">​</a></h2><p><strong>Three parts of the interface look the way they do because of this tool.</strong></p><ul><li><p><strong><code>matread</code> takes the type.</strong> A function may not return more than one type. The type of a variable comes from the file. So the caller states it. Use <code>@matload</code> to list several variables at once without repeating the call; it writes the same typed reads for you.</p></li><li><p><strong><code>matsize</code> returns a <code>Vector{Int}</code>.</strong> The number of dimensions comes from the file. A tuple would then have no fixed length.</p></li><li><p><strong>A cell array returns marks.</strong> The items have mixed types. One mark type covers them all.</p></li></ul><p>Two rules inside the code follow from the same tool. They are worth knowing if you read the source.</p><ul><li><p>The list of filters is a fixed-size group of numbers. It is not a list of filter objects. A container whose length or contents come from the file cannot be worked out in advance.</p></li><li><p>No helper function reads a variable that was set inside an <code>if</code> and then used inside an inner function. Julia puts such a variable in a box. The build then loses track of its type.</p></li></ul><h2 id="Two-checks,-not-one" tabindex="-1">Two checks, not one <a class="header-anchor" href="#Two-checks,-not-one" aria-label="Permalink to &quot;Two checks, not one {#Two-checks,-not-one}&quot;">​</a></h2><p><strong>Both checks run in the build service. They cover different things.</strong></p><p><code>trim/trimcheck.jl</code> runs the compiler check on every entry point.</p><ul><li><p>It uses the same compiler step the real build uses.</p></li><li><p>It applies the same patches to the base library.</p></li><li><p>It therefore reports the same problems, including problems inside cleanup functions.</p></li><li><p>A failure here is a failure of the real build.</p></li><li><p>It lives outside the test setup, in its own environment. The check needs a part of Julia that the tests have no reason to carry.</p></li></ul><p><code>juliac/build.jl</code> builds a real program and runs it.</p><ul><li><p>It links the program. The first check does not.</p></li><li><p>It starts the program. The first check does not.</p></li><li><p>It keeps every start-up function in every loaded package. The first check keeps only the one function you name.</p></li></ul><p>Both gaps have caused real failures in code that passed the first check:</p><ul><li><p>a library that looks up files on disk when it starts, and stops the program before it runs</p></li><li><p>a call into a C library, written in a form that passes the check and then fails at run time</p></li></ul><p>Run both:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>julia --project=trim -e &#39;using Pkg; Pkg.develop(path=&quot;.&quot;); Pkg.instantiate()&#39;</span></span>
<span class="line"><span>julia --project=trim trim/trimcheck.jl</span></span>
<span class="line"><span>julia --project=. juliac/build.jl</span></span></code></pre></div><h2 id="Dependencies" tabindex="-1">Dependencies <a class="header-anchor" href="#Dependencies" aria-label="Permalink to &quot;Dependencies {#Dependencies}&quot;">​</a></h2><p><strong>This package depends on 2 things only: <code>Mmap</code> and <code>ChunkCodecLibZlib</code>.</strong></p><p><code>ChunkCodecLibZlib</code> calls the zlib library, for compressed data. On Julia 1.12 that wrapper is a small piece of the standard library. It names the library by a fixed name. It does not look up any files on disk.</p><p>So a small compiled program finds zlib through a path that <code>juliac</code> already writes into it. It gains no new outside dependency. The name <code>libz.so.1</code> does not even appear in the list of libraries the program needs.</p><p><strong>This is why new dependencies need care here.</strong> Some wrappers look up their library on disk when the program starts. If the program cannot find it, the program stops before your code runs.</p>`,32)])])}const u=s(n,[["render",o]]);export{k as __pageData,u as default};
