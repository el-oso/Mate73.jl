import{_ as s,o as a,c as e,ao as i}from"./chunks/framework.BdOmiPte.js";const k=JSON.parse('{"title":"The format","description":"","frontmatter":{},"headers":[],"relativePath":"format.md","filePath":"format.md","lastUpdated":null}'),n={name:"format.md"};function h(o,t,l,r,d,p){return a(),e("div",null,[...t[0]||(t[0]=[i(`<h1 id="The-format" tabindex="-1">The format <a class="header-anchor" href="#The-format" aria-label="Permalink to &quot;The format {#The-format}&quot;">​</a></h1><p><strong>A version 7.3 <code>.mat</code> file is an HDF5 file with a MATLAB label in front and MATLAB notes beside each array.</strong></p><p>HDF5 is a general file format for arrays. It says how the bytes are stored. It does not say what MATLAB means by them. The notes beside each array carry that meaning. Their names all start with <code>MATLAB_</code>.</p><h2 id="What-this-package-reads" tabindex="-1">What this package reads <a class="header-anchor" href="#What-this-package-reads" aria-label="Permalink to &quot;What this package reads {#What-this-package-reads}&quot;">​</a></h2><table tabindex="0"><thead><tr><th style="text-align:right;">MATLAB type</th><th style="text-align:right;">Julia type</th></tr></thead><tbody><tr><td style="text-align:right;"><code>double</code>, <code>single</code></td><td style="text-align:right;"><code>Array{Float64,N}</code>, <code>Array{Float32,N}</code></td></tr><tr><td style="text-align:right;"><code>int8</code> to <code>int64</code>, <code>uint8</code> to <code>uint64</code></td><td style="text-align:right;"><code>Array{T,N}</code></td></tr><tr><td style="text-align:right;"><code>logical</code></td><td style="text-align:right;"><code>Array{Bool,N}</code></td></tr><tr><td style="text-align:right;">complex numbers</td><td style="text-align:right;"><code>Array{Complex{T},N}</code></td></tr><tr><td style="text-align:right;"><code>char</code>, one row</td><td style="text-align:right;"><code>String</code></td></tr><tr><td style="text-align:right;"><code>char</code>, any shape</td><td style="text-align:right;"><code>Array{Char,N}</code>, as 16-bit units</td></tr><tr><td style="text-align:right;">empty arrays</td><td style="text-align:right;">the shape stays, so a <code>0x3</code> stays <code>0x3</code></td></tr><tr><td style="text-align:right;"><code>cell</code></td><td style="text-align:right;"><code>Array{MatRef,N}</code>, one mark for each item</td></tr><tr><td style="text-align:right;"><code>struct</code></td><td style="text-align:right;">fields by path; <code>matkeys</code> gives the names</td></tr><tr><td style="text-align:right;">struct arrays</td><td style="text-align:right;">each field is an <code>Array{MatRef,N}</code></td></tr><tr><td style="text-align:right;">objects of a class you wrote</td><td style="text-align:right;">properties by path</td></tr><tr><td style="text-align:right;"><code>datetime</code></td><td style="text-align:right;"><code>Array{DateTime,N}</code></td></tr><tr><td style="text-align:right;"><code>string</code>, <code>categorical</code></td><td style="text-align:right;"><code>Array{String,N}</code></td></tr><tr><td style="text-align:right;"><code>table</code></td><td style="text-align:right;">a <code>NamedTuple</code> of columns</td></tr><tr><td style="text-align:right;">function handles</td><td style="text-align:right;">the values MATLAB stored, by path</td></tr><tr><td style="text-align:right;">objects of a class written before 2008</td><td style="text-align:right;">properties by path</td></tr></tbody></table><h2 id="Boxes-with-mixed-contents" tabindex="-1">Boxes with mixed contents <a class="header-anchor" href="#Boxes-with-mixed-contents" aria-label="Permalink to &quot;Boxes with mixed contents {#Boxes-with-mixed-contents}&quot;">​</a></h2><p><strong>A cell array gives you marks, not values. You follow one mark at a time.</strong></p><p>Think of a cloakroom. You get a numbered ticket, not the coat. You hand back one ticket and get one coat.</p><p>The items in a cell array can have different types. The same is true for each field of a struct array. A function that returned all of them at once would have no single type. So you get one mark for each item. The mark type is <a href="/MATTE73.jl/dev/api#MATTE73.MatRef"><code>MatRef</code></a>.</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">cells </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> matread</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(f, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;c&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, Matrix{MatRef})</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">matclass</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(f, cells[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">])                      </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># look before you choose a type</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">matread</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(f, cells[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], Matrix{Float64})      </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># follow the mark</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">matkeys</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(f, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;s&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)                            </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># the field names of a struct</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">matread</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(f, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;s/a&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, Matrix{Float64})         </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># one field, by path</span></span></code></pre></div><p>A cell inside a cell gives you more marks. The steps stay the same. Paths can also go deeper, such as <code>&quot;s/inner/a&quot;</code>.</p><h3 id="A-path-that-starts-at-a-mark" tabindex="-1">A path that starts at a mark <a class="header-anchor" href="#A-path-that-starts-at-a-mark" aria-label="Permalink to &quot;A path that starts at a mark {#A-path-that-starts-at-a-mark}&quot;">​</a></h3><p><strong>A path can start at a mark instead of at the top of the file.</strong></p><p>This matters for a cell of structs, which is a common shape. The cell gives you a mark for each struct. From there you name the field you want:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">runs </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> matread</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(f, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;runs&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, Matrix{MatRef})</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">one </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> runs[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">matread</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(f, one, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;count&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, Float64)                </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># one number</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">matread</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(f, one, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;inner/values&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, Matrix{Float64}) </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># a field of a field</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">matread</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(f, one, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;parts/name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, Matrix{MatRef})    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># a cell field, so more marks</span></span></code></pre></div><p>The path works the same way it does at the top of the file. It goes down as many steps as you write. Every type the 2-argument form takes is taken here as well, and the checks are the same ones.</p><p>A plain number type means a MATLAB scalar. You get the value, not the 1x1 array.</p><p><a href="/MATTE73.jl/dev/api#MATTE73.matref"><code>matref</code></a> steps down without reading, for a place you want to use more than once:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">inner </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> matref</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(f, one, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;inner&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">matread</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(f, inner, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;values&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, Matrix{Float64})</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">matread</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(f, inner, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;extra&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, Matrix{Float64})</span></span></code></pre></div><h2 id="Objects-of-a-class" tabindex="-1">Objects of a class <a class="header-anchor" href="#Objects-of-a-class" aria-label="Permalink to &quot;Objects of a class {#Objects-of-a-class}&quot;">​</a></h2><p><strong>An object holds no data. It holds numbers that point into a table.</strong></p><p>Think of a library catalogue card. The card is not the book. The card tells you the shelf.</p><p>MATLAB keeps that table in a hidden entry named <code>#subsystem#</code>. MathWorks does not publish the layout of the table. People worked it out by reading files. The notes they wrote are <a href="https://github.com/foreverallama/matio/blob/main/docs/subsystem_data_format.md" target="_blank" rel="noreferrer">here</a>.</p><p>This package follows the numbers for you. An object then behaves like a struct.</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">matobjectclass</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(f, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;obj&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)             </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># &quot;TestClasses.BasicClass&quot;, with the namespace</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">matkeys</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(f, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;obj&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)                    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># the property names</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">matread</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(f, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;obj/a&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, Matrix{Float64}) </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># one property, by path</span></span></code></pre></div><p>Properties added later with <code>addprop</code> are in the list too. Each one is itself a small object. It holds the name you gave and the value. This package follows that step as well.</p><p><code>matclass</code> still reports <code>MAT_UNSUPPORTED</code> for an object. That function answers one question: is this a plain array? An object is not. Ask <code>matobjectclass</code> instead.</p><p>An object held by another object is found too. MATLAB writes the note that marks an object only at the top level. Inside the hidden entry it writes none, so the only mark left is the tag the index array starts with. This package reads that tag, as MATLAB does.</p><h3 id="Dates" tabindex="-1">Dates <a class="header-anchor" href="#Dates" aria-label="Permalink to &quot;Dates {#Dates}&quot;">​</a></h3><p>A MATLAB <code>datetime</code> is an object too, but this package knows the rule for it. Its <code>data</code> property counts milliseconds from 1 January 1970, so it reads as a <code>DateTime</code>:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Dates</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">t </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> matread</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(f, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;when&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, Matrix{DateTime})   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># or matread(f, &quot;when&quot;)</span></span></code></pre></div><p>A time zone on the MATLAB side is not applied. MATLAB may store a step smaller than a millisecond, which a <code>DateTime</code> cannot hold, and that part is dropped.</p><h3 id="Text-held-as-a-string" tabindex="-1">Text held as a <code>string</code> <a class="header-anchor" href="#Text-held-as-a-string" aria-label="Permalink to &quot;Text held as a \`string\` {#Text-held-as-a-string}&quot;">​</a></h3><p>A MATLAB <code>string</code> is an object as well, and this package knows its rule too.</p><p>A <code>string</code> is not a <code>char</code>. MATLAB keeps a <code>char</code> array as text beside the array. It keeps a <code>string</code> array as one block of numbers, holding in order: a version, the number of dimensions, the dimensions, the length of each piece of text, and then all the text back to back as 16-bit units.</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">names </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> matread</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(f, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;names&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, Matrix{String})   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># or matread(f, &quot;names&quot;)</span></span></code></pre></div><p>You get one <code>String</code> for each element, in the shape MATLAB used.</p><p>A <code>categorical</code> reads as text too. MATLAB keeps a list of the choices and one number for each element, counting into that list. You get the text of the choice.</p><h3 id="Tables" tabindex="-1">Tables <a class="header-anchor" href="#Tables" aria-label="Permalink to &quot;Tables {#Tables}&quot;">​</a></h3><p><strong>A table reads as a named tuple of columns.</strong></p><p>Think of a filing cabinet. Each drawer has a label on the front and holds one kind of paper. You take out a whole drawer, not one sheet.</p><p>A named tuple of vectors is already a column table, so anything that reads the Tables.jl interface takes the result as it is. This package needs no dependency for that.</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">t </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> matread</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(f, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;flights&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)     </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># every column, with the types taken from the file</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">t</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Customer</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> DataFrames</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">DataFrame</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(t)</span></span></code></pre></div><p>For a small compiled program you state the columns and their types. Each type is the type of a whole column, so it is a <code>Vector</code>:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">t </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> matread</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(f, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;flights&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, NamedTuple{</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">:FlightNum</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">:Customer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Tuple{Vector{Float64}, Vector{String}},</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span></code></pre></div><p>You may ask for some of the columns only, in any order. A name the table does not have stops with an error.</p><h3 id="Function-handles" tabindex="-1">Function handles <a class="header-anchor" href="#Function-handles" aria-label="Permalink to &quot;Function handles {#Function-handles}&quot;">​</a></h3><p><strong>A saved function handle holds no code. It holds the name, or the text you typed.</strong></p><p>Think of a phone number written on a card. The card does not ring. Someone has to dial it.</p><p>MATLAB stores a named handle such as <code>@sin</code> as the name <code>sin</code>. It stores an anonymous handle such as <code>@(x) x</code> as the text of the expression, together with the values it captured when you made it. It also stores the folder MATLAB was installed in.</p><p>So you can read what a saved file referred to. You cannot call it from Julia; there is nothing to call.</p><p>A handle reads as a set of named values, like a struct:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">matkeys</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(f, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;h&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)                            </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># [&quot;function_handle&quot;, &quot;matlabroot&quot;, ...]</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">matread</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(f, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;h/function_handle/function&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, String)</span></span></code></pre></div><p><code>matobjectclass</code> gives an empty string for a handle. A handle carries the note that marks an object, but it is not a <code>classdef</code> instance, so it has no class name in the hidden table.</p><h2 id="Empty-arrays" tabindex="-1">Empty arrays <a class="header-anchor" href="#Empty-arrays" aria-label="Permalink to &quot;Empty arrays {#Empty-arrays}&quot;">​</a></h2><p><strong>An empty array keeps its shape.</strong></p><p>MATLAB does not store zero items. It stores a short list of the dimensions, with a note named <code>MATLAB_empty</code>. So a <code>0x3</code> array stays a <code>0x3</code> array.</p><h2 id="Two-notes-do-the-work" tabindex="-1">Two notes do the work <a class="header-anchor" href="#Two-notes-do-the-work" aria-label="Permalink to &quot;Two notes do the work {#Two-notes-do-the-work}&quot;">​</a></h2><p>HDF5 cannot tell a true-or-false value from a small whole number. They have the same bytes. Two MATLAB notes settle it:</p><ul><li><p><code>MATLAB_class</code> gives the MATLAB type name, such as <code>double</code>.</p></li><li><p><code>MATLAB_int_decode</code> marks <code>logical</code> with a 1 and <code>char</code> with a 2.</p></li></ul><p>If you read a <code>logical</code> array as <code>UInt8</code>, the package stops with an error. The reverse also stops. Neither case is a silent success.</p><h2 id="How-the-bytes-are-stored" tabindex="-1">How the bytes are stored <a class="header-anchor" href="#How-the-bytes-are-stored" aria-label="Permalink to &quot;How the bytes are stored {#How-the-bytes-are-stored}&quot;">​</a></h2><p><strong>Two programs write this format in 2 different shapes. This package reads both.</strong></p><table tabindex="0"><thead><tr><th style="text-align:right;"></th><th style="text-align:right;">MATLAB writes</th><th style="text-align:right;">The HDF5 C library writes</th></tr></thead><tbody><tr><td style="text-align:right;">Superblock</td><td style="text-align:right;">version 0</td><td style="text-align:right;">version 2</td></tr><tr><td style="text-align:right;">Object headers</td><td style="text-align:right;">version 1</td><td style="text-align:right;">version 2</td></tr><tr><td style="text-align:right;">Groups</td><td style="text-align:right;">local heap and a version 1 tree</td><td style="text-align:right;">link messages</td></tr></tbody></table><p>MATLAB writes the left column. The HDF5 C library writes the right column. HDF5.jl, MAT.jl and this package all write the right column. MATLAB reads both.</p><p>The bytes of an array sit in one of 3 layouts:</p><ul><li><p><strong>compact</strong>, inside the header, for very small arrays</p></li><li><p><strong>contiguous</strong>, in one block</p></li><li><p><strong>chunked</strong>, in blocks, and often compressed</p></li></ul><p><strong>Most real arrays use the chunked layout.</strong> MATLAB compresses anything above a few hundred bytes. Only the smallest variables are compact. This package reads chunked data with the deflate and shuffle filters.</p><h2 id="What-this-package-writes" tabindex="-1">What this package writes <a class="header-anchor" href="#What-this-package-writes" aria-label="Permalink to &quot;What this package writes {#What-this-package-writes}&quot;">​</a></h2><p><code>matwrite</code> writes the same shape as the HDF5 C library:</p><ul><li><p>a 512-byte block in front, holding the MATLAB label</p></li><li><p>superblock version 2</p></li><li><p>version 2 object headers</p></li><li><p>a root group made of link messages</p></li><li><p>arrays in one block each, with no compression</p></li><li><p>the <code>MATLAB_</code> notes beside each array</p></li></ul><p>Version 2 parts each carry a running total of their own bytes. The HDF5 C library checks these totals. A wrong total is rejected. It is not accepted quietly.</p><h3 id="What-you-can-write" tabindex="-1">What you can write <a class="header-anchor" href="#What-you-can-write" aria-label="Permalink to &quot;What you can write {#What-you-can-write}&quot;">​</a></h3><table tabindex="0"><thead><tr><th style="text-align:right;">you give</th><th style="text-align:right;">MATLAB sees</th></tr></thead><tbody><tr><td style="text-align:right;">an <code>Array</code> of numbers or <code>Bool</code></td><td style="text-align:right;">an array of the matching type</td></tr><tr><td style="text-align:right;">one number or <code>Bool</code></td><td style="text-align:right;">a 1x1 array</td></tr><tr><td style="text-align:right;">a <code>String</code></td><td style="text-align:right;">text</td></tr><tr><td style="text-align:right;">a <code>NamedTuple</code></td><td style="text-align:right;">a struct, one field per name</td></tr><tr><td style="text-align:right;">a <code>Tuple</code></td><td style="text-align:right;">a cell array of one row</td></tr></tbody></table><p><strong>A named tuple becomes a struct. A tuple becomes a cell array. Both nest.</strong></p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">matwrite</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;run.mat&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;cfg&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  =&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (gain </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 2.5</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, mode </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;fast&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, limits </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)),</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;runs&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ([</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1.0</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 2.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;second&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, (name </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;third&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, ok </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>Every type is settled while your program is compiled, so writing works inside a small compiled program, as reading does.</p><p>A struct is a group holding one member for each field, so a struct inside a struct is a group inside a group.</p><p>A cell holds no values. It holds the address of each item, as a library catalogue card holds a shelf number. The items themselves are written in a hidden entry named <code>#refs#</code>, which is where MATLAB puts them too. An item must be linked under some name, or nothing could walk to it.</p><p>This package does not compress. Files are therefore larger than the files MATLAB writes.</p><h2 id="What-this-package-does-not-do" tabindex="-1">What this package does not do <a class="header-anchor" href="#What-this-package-does-not-do" aria-label="Permalink to &quot;What this package does not do {#What-this-package-does-not-do}&quot;">​</a></h2><p>Two limits are quiet. The package gives an answer, but not the answer you asked for. These come first, because a quiet limit is easy to miss.</p><ul><li><p><strong>The order of struct fields.</strong> <code>matkeys</code> gives the names in file order. MATLAB may use a different order. The names are correct. The values are correct. Only the order can differ. MATLAB keeps its order in a note named <code>MATLAB_fields</code>. That note uses a storage area this package does not read yet.</p></li><li><p><strong>Arrays of objects.</strong> One variable can point to many objects. This package uses the first one. <code>matobjectclass</code>, <code>matkeys</code> and a property path all describe that first object.</p></li></ul><p>The other limits stop with an error, or report <code>MAT_UNSUPPORTED</code>.</p><ul><li><p><strong>Sparse arrays.</strong> When this is added, <code>SparseArrays</code> must be an optional dependency. It pulls in a library that looks up files on disk when it starts. A small compiled program then stops before it runs, if it cannot find those files. So it must stay off the normal path.</p></li><li><p><strong>Objects of the MATLAB types</strong> <code>duration</code> and <code>calendarDuration</code>. These are objects like any other. Their class and their properties read correctly. Building the value that MATLAB shows needs a rule for each type, and only <code>datetime</code>, <code>string</code>, <code>categorical</code> and <code>table</code> have one so far. Classes you write yourself need no such rule.</p></li><li><p><strong>Calling a function handle.</strong> The file holds a name, or the text of an expression. Turning either one into something you can call is a job for a different tool.</p></li><li><p><strong>An element of a <code>categorical</code> with no choice.</strong> MATLAB shows it as <code>&lt;undefined&gt;</code>. There is no text for it, so this stops with an error rather than giving back an empty one.</p></li><li><p><strong>The row names of a table.</strong> Only the columns are read.</p></li><li><p><strong>The default value of a property.</strong> A class can give a property a value in its own definition. MATLAB keeps those values in a part of the table this package does not read yet, so an object that never set the property lists no property at all.</p></li><li><p><strong>Properties kept in the table.</strong> Most properties point to a value elsewhere. Some small ones sit in the table itself. Only the first kind has an address to follow, so the second kind stops with an error.</p></li><li><p><strong>Text above code point 65535.</strong> MATLAB keeps text as 16-bit units. <code>Array{Char,N}</code> gives you those units, one for one. One rare character then arrives as 2 units. The <code>String</code> method joins them correctly. MAT.jl gives one <code>String</code> for each row instead. This package gives what MATLAB stores.</p></li><li><p><strong>Compression when writing.</strong> Arrays are written in one block each.</p></li><li><p><strong>Writing a cell array with no items.</strong> A cell of one row needs at least one item, so an empty tuple stops with an error rather than writing a shape MATLAB may not accept.</p></li><li><p><strong>Writing complex numbers, sparse arrays, <code>datetime</code>, <code>string</code> or objects.</strong> These read, but nothing writes them yet.</p></li></ul><p><code>keys(f)</code> also lists 2 names that MATLAB uses for itself: <code>#refs#</code> and <code>#subsystem#</code>. They hold the contents of cells and the object tables. Skip them if you only want your own variables.</p><p>The package does not read some file layouts: fractal heap groups, superblock versions 1 and 3, and filters other than deflate and shuffle. MATLAB does not write these. Other tools can.</p><p>Two limits are set by fixed sizes in the code: 8 dimensions for an array, and 4 filters for one array. MATLAB stays far below both.</p><h2 id="Tests" tabindex="-1">Tests <a class="header-anchor" href="#Tests" aria-label="Permalink to &quot;Tests {#Tests}&quot;">​</a></h2><p><strong>Every value is compared against a second, independent reader.</strong></p><p>The tests use MAT.jl. That package reads through the HDF5 C library. It shares no code with this one. The test files come from MATLAB itself.</p><p>Files that this package writes are read back through the same C library. That library checks the running totals and the structure. A bad file fails there. A file cannot pass by being wrong in the same way twice.</p>`,92)])])}const g=s(n,[["render",h]]);export{k as __pageData,g as default};
